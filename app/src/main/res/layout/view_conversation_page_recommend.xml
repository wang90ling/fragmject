<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingHorizontal="7.5dp"
    android:paddingTop="9dp">

    <ImageView
        android:id="@+id/like_icon"
        android:layout_width="29dp"
        android:layout_height="0dp"
        android:layout_marginStart="7.5dp"
        android:focusable="true"
        android:src="@drawable/ic_conversation_like"
        app:layout_constraintDimensionRatio="1:1"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/like_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="6dp"
        android:gravity="center_vertical"
        android:includeFontPadding="false"
        android:text="@string/conversation_like_title"
        android:textColor="@color/color_85_000000"
        android:textSize="15dp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="@+id/like_icon"
        app:layout_constraintStart_toEndOf="@+id/like_icon"
        app:layout_constraintTop_toTopOf="@+id/like_icon" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/con_like_recycler"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="7.5dp"
        android:layout_marginTop="10dp"
        android:focusableInTouchMode="true"
        android:scrollbars="none"
        app:layoutManager="androidx.recyclerview.widget.GridLayoutManager"
        app:layout_constrainedWidth="true"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/like_title"
        app:spanCount="5"
        tools:listitem="@layout/item_conversation_recommend" />
</androidx.constraintlayout.widget.ConstraintLayout>