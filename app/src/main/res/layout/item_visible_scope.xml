<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="58dp"
    android:layout_marginHorizontal="13dp">

    <ImageView
        android:id="@+id/iv_visible_scope_selected"
        android:layout_width="23dp"
        android:layout_height="23dp"
        android:src="@drawable/ic_visible_scope_selected"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tv_visible_scope"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:includeFontPadding="false"
        android:textColor="#d9010101"
        android:textSize="13sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@id/iv_visible_scope_selected"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="公开" />

    <!--    <TextView
            android:id="@+id/tv_visible_scope_desc"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:includeFontPadding="false"
            android:textColor="#66010101"
            android:textSize="12sp"
            app:layout_constraintStart_toStartOf="@id/tv_visible_scope"
            app:layout_constraintTop_toBottomOf="@id/tv_visible_scope"
            tools:text="发布后将对所有人可见" />-->
</androidx.constraintlayout.widget.ConstraintLayout>